Currently Working Driver Ports (for OS/2 ecosystem)
The osFree project already has working ports including Uniaudio (a port of ALSA for sound drivers), os2ahci (a port of Linux AHCI driver), multimac (port of Linux NIC drivers), and GenMAC (a wrapper for Windows wifi/NIC drivers). Osfree
